import type { Config } from 'prettier';

const config: Config = {
  tabWidth: 2,
  printWidth: 120,
  trailingComma: 'all',
  endOfLine: 'lf',
  semi: true,
  singleQuote: true,
};

export default config;
